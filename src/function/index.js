console.log(`function`);

export const func = {
  // lnbActive: e => {
  // const item = ;
  // if (e.target.className === "mouseover") {
  // const top = e.clientY - e.offsetY + e.target.offsetHeight
  // const totalHeight = top + e.target.querySelectorAll(':scope > .tooltip')[0].offsetHeight
  // e.target.querySelectorAll(':scope > .tooltip')[0].style.display = 'block'
  // e.target.querySelectorAll(':scope > .tooltip')[0].style.top =
  //   totalHeight > window.innerHeight ? top - (totalHeight - window.innerHeight + 10) + 'px' : top + 'px'
  // e.target.querySelectorAll(':scope > .tooltip')[0].style.left =
  //   e.clientX - e.offsetX - e.target.querySelectorAll(':scope > .tooltip')[0].offsetWidth + e.target.offsetWidth + 'px'
  // const cancelOver = () => {
  //   e.target.querySelectorAll(':scope > .tooltip')[0].style.display = 'none'
  //   document.removeEventListener('scroll', cancelOver)
  // }
  // document.addEventListener('scroll', cancelOver)
  // }
  // },
};
